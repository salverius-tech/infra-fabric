{
    email {{ caddy_email }}
}

{{ hermes_domain }} {
    encode zstd gzip
    root * /var/www/hermes
    file_server

    tls {
        dns cloudflare {env.CF_DNS_API_TOKEN}
        resolvers 1.1.1.1
    }
}
