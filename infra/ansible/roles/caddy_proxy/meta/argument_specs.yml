---
argument_specs:
  main:
    short_description: "Configure a Caddy reverse proxy and TLS policy on a DNS/utility host."
    options:
      technitium_vmid:
        type: int
        required: true
        description: "LXC VMID used by the host role for required task gating."
      caddy_server_name:
        type: str
        required: false
        description: "Primary server name for the main TLS site; must be set when caddy_server_names is not provided."
      caddy_server_names:
        type: list
        required: false
        description: "Alternate list of server names for the main site; must be set when caddy_server_name is not provided."
      caddy_upstream:
        type: str
        required: true
        description: "Upstream target (host:port) for the main reverse-proxy route."
      caddy_email:
        type: str
        required: true
        description: "Contact email used in the Caddy TLS block."
      caddy_cloudflare_api_token:
        type: str
        required: true
        no_log: true
        description: "Cloudflare DNS API token used by Caddy's DNS challenge provider."
      caddy_extra_vhosts:
        type: list
        required: false
        description: "Optional extra vhosts, each with server_name/server_names and upstream (default: [])."
      caddy_proxy_caddy_cloudflare_version:
        type: str
        required: false
        description: "Reviewed Caddy artifact version."
      caddy_proxy_caddy_cloudflare_sha256_amd64:
        type: str
        required: false
        description: "Lowercase SHA-256 for the reviewed amd64 Caddy artifact."
      caddy_proxy_caddy_cloudflare_sha256_arm64:
        type: str
        required: false
        description: "Lowercase SHA-256 for the reviewed arm64 Caddy artifact."
      caddy_proxy_artifact_cache_root:
        type: str
        required: false
        description: "Controller-side reviewed artifact cache root."
